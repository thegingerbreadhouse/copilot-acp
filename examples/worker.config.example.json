{
  "id": "worker.dev.1",
  "mailbox": "worker.dev",
  "database": "../copilot-acp.sqlite",
  "system_prompt": "Implement requested work carefully, keep responses concise, prefer readable maintainable solutions, preserve existing architecture unless the task explicitly requests a refactor, and state blockers clearly in the result body if the task cannot be completed.",
  "session": {
    "cwd": "..",
    "executable": "/opt/homebrew/bin/copilot",
    "env": {},
    "extra_cli_args": [
      "--add-dir",
      ".."
    ]
  },
  "runtime": {
    "poll_interval_seconds": 1.0,
    "lease_seconds": 300
  }
}
